
<project name="Ant-Test" default="main" basedir=".">
	<!-- Sets variables which can later be used. -->
	<!-- The value of a property is accessed via ${} -->
	<property name="src.dir" location="src" />
	<property name="build.dir" location="bin" />
	<property name="dist.dir" location="dist" />
	<property name="docs.dir" location="javadoc" />
	<property name="project-name" value="jat" />
	<property name="folder-to-zip" value="." />
	
	<!-- Deletes the existing build, docs and dist directory-->
	<target name="clean">
		<delete dir="${docs.dir}" />
		<delete dir="${dist.dir}" />
		<delete dir="zip" />
	</target>

	<!-- Creates the  build, docs and dist directory-->
	<target name="makedir">
		<mkdir dir="${docs.dir}" />
		<mkdir dir="${dist.dir}" />
		<mkdir dir="zip" />
	</target>

	<!--JAT Core Creates the deployable jar file  -->
	<target name="jatcore.jar" depends="clean,makedir">
		<jar destfile="${dist.dir}\jatcore.jar" basedir="${build.dir}" excludes="**/application/,**/examples/,**/simulation/,**/tests/,**/unittest/,**/org/"  >
			<manifest>
				<attribute name="Main-Class" value="test.Main" />
			</manifest>
		</jar>
	</target>

	<!--JAT Application Creates the deployable jar file  -->
	<target name="jatapplication.jar" depends="clean,makedir">
		<jar destfile="${dist.dir}\jatapplication.jar" basedir="${build.dir}" excludes="**/core/,**/examples/,**/simulation/,**/tests/,**/unittest/,**/org/"  >
			<manifest>
				<attribute name="Main-Class" value="test.Main" />
			</manifest>
		</jar>
	</target>

	<!--JAT Examples Creates the deployable jar file  -->
	<target name="jatexamples.jar" depends="clean,makedir">
		<jar destfile="${dist.dir}\jatexamples.jar" basedir="${build.dir}" excludes="**/application/,**/core/,**/simulation/,**/tests/,**/unittest/,**/org/"  >
			<manifest>
				<attribute name="Main-Class" value="test.Main" />
			</manifest>
		</jar>
	</target>

	<!--JAT JMathTools Creates the deployable jar file  -->
	<target name="jatjmathtools.jar" depends="clean,makedir">
		<jar destfile="${dist.dir}\jatjmathtools.jar" basedir="${build.dir}" excludes="**/application/,**/core/,**/examples/,**/simulation/,**/tests/,**/unittest/"  >
			<manifest>
				<attribute name="Main-Class" value="test.Main" />
			</manifest>
		</jar>
	</target>

	
	<target name="zip">
			<zip destfile="zip/jat.zip" basedir="${folder-to-zip}" excludes="**/.settings/,**/bin/,**/data/jat3D/,**/docs/,**/input/,**/javadoc/,**/scripts/,**/src/,**/zip/" />
		</target>
			
			
	<target name="main" depends="jatcore.jar, jatapplication.jar, jatexamples.jar, jatjmathtools.jar, zip">
		<description>Main target</description>
	</target>

</project>
